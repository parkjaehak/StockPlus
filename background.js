// background.js
// 추후 한국투자증권 API WebSocket 또는 REST 연동 구현 예정
// 예시: WebSocket 연결 후 popup.js로 데이터 전달 구조

// chrome.runtime.onMessage.addListener((msg, sender, sendResponse) => {
//   if (msg.type === 'GET_STOCK_DATA') {
//     // API 호출 또는 WebSocket 데이터 전달
//     sendResponse({ data: ... });
//   }
// });
